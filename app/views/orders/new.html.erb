<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>New Order</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    .card {
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .btn-slightly-circular {
      border-radius: 25px; /* Adjust this value for the desired roundness */
      padding: 9.5px 13px; /* Adjust these values as needed */
    }
    
    .card-header {
      background-color: #007bff;
      color: white;
      border-top-left-radius: 15px;
      border-top-right-radius: 15px;
      padding: 1rem 1.5rem;
    }

    .card-header h1 {
      margin: 0;
      font-size: 1.75rem;
    }

    .form-label {
      font-weight: bold;
    }

    .btn-primary {
      border-radius: 50px;
      padding: 0.5rem 1.5rem;
      font-size: 1rem;
    }

    .btn-primary:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="card">
          <div class="card-header">
            <h1>New Order</h1>
          </div>
          <div class="card-body">
            <%= form_with model: @order, local: true do |form| %>
              <div class="form-group mb-3">
                <%= form.label :full_name, class: "form-label" %>
                <%= form.text_field :full_name, class: "form-control" %>
              </div>

              <div class="form-group mb-3">
                <%= form.label :phone_number, class: "form-label" %>
                <%= form.text_field :phone_number, class: "form-control" %>
              </div>

              <div class="form-group mb-3">
                <%= form.label :address, class: "form-label" %>
                <%= form.text_field :address, class: "form-control" %>
              </div>

              <div class="form-group mb-3">
                <%= form.label :guests_number, class: "form-label" %>
                <%= form.number_field :guests_number, class: "form-control" %>
              </div>

              <div class="form-group mb-3">
                <%= form.label :date, class: "form-label" %>
                <%= form.date_field :date, class: "form-control" %>
              </div>

              <div class="form-group mb-4">
                <%= form.label :description, 'Anything else?', class: "form-label" %>
                <%= form.text_area :description, class: "form-control" %>
              </div>

              <div >
                <%= form.submit 'Place Order', class: "btn btn-primary me-2" %>
            <%= link_to 'Back to Cart', cart_path, class: "btn btn-secondary btn-slightly-circular" %>

              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
