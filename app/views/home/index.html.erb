<!DOCTYPE html>
<html>
<head>
  <title>Kitchen Page</title>
  <link rel="stylesheet" href="<%= asset_path 'application.css' %>">
  <style>
    .kitchen-info {
      display: flex;
      justify-content: space-between;
      margin: 20px 0;
    }
    .card {
      padding: 20px;
      background: #f8f9fa;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }
    .menus {
      display: flex;
      overflow-x: auto;
      scroll-behavior: smooth;
    }
    .menu-item {
      min-width: 200px;
      margin-right: 20px;
    }
    .scroll-buttons {
      display: flex;
      justify-content: space-between;
      width: 100%;
    }
    .scroll-buttons button {
      background: none;
      border: none;
      font-size: 2em;
      cursor: pointer;
    }
    .reviews {
      margin: 20px 0;
    }
    .contact-info {
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="kitchen-info">
      <div class="info">
        <h2>About Our Kitchen</h2>
        <p>Some information about the kitchen...</p>
      </div>
      <div class="card">
        <img src="path/to/kitchen-image.jpg" alt="Kitchen Image">
      </div>
    </div>

    <div class="menus-section">
      <h1>Our Menus</h1>
      <div class="scroll-buttons">
        <button onclick="scrollLeft()">&#10094;</button>
        <button onclick="scrollRight()">&#10095;</button>
      </div>
      <div class="menus" id="menu-container">
        <% @prebuilt_menus.each do |menu| %>
          <div class="card menu-item">
            <h3><%= menu.name %></h3>
            <p>descriptions</p>
            <button onclick="location.href='<%= new_user_session_path %>'">Order Now</button>
          </div>
        <% end %>
      </div>
    </div>

    <div class="create-menu">
      <h1>Create Your Own Menu</h1>
      <button onclick="location.href='<%= new_user_session_path %>'">Create Menu</button>
    </div>

    <div class="reviews">
      <h2>User Reviews</h2>
      <div class="card">
        <img src="path/to/user-image.jpg" alt="User Image">
        <h4>User Name</h4>
        <p>Great service and delicious food!</p>
      </div>
      <!-- Add more reviews as needed -->
    </div>

    <div class="contact-info">
      <h2>Contact Us</h2>
      <p>Address: Your Address Here</p>
      <p>Phone: Your Phone Number Here</p>
      <p>WhatsApp: Your WhatsApp Number Here</p>
      <p>Email: Your Email Here</p>
    </div>
  </div>

  <script>
    function scrollLeft() {
      document.getElementById('menu-container').scrollBy({
        left: -200,
        behavior: 'smooth'
      });
    }

    function scrollRight() {
      document.getElementById('menu-container').scrollBy({
        left: 200,
        behavior: 'smooth'
      });
    }

    setInterval(() => {
      document.getElementById('menu-container').scrollBy({
        left: 200,
        behavior: 'smooth'
      });
    }, 3000);
  </script>
</body>
</html>
