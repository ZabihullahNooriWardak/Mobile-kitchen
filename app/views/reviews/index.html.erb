<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reviews</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    .header {
      background-color: #343a40;
      color: white;
      padding: 15px;
      border-radius:5px;
    }
    .card {
      border-radius: 15px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin-bottom: 10px; /* Adjust the margin to reduce space between cards */
    }
    .btn-gradient {
      background: linear-gradient(45deg, #007bff, #0056b3);
      border: none;
      color: white;
      border-radius: 25px;
    }
    .btn-gradient:hover {
      background: linear-gradient(45deg, #0056b3, #007bff);
      color: white;
    }
    .btn-danger {
      border-radius: 25px;
    }
    .card-body {
      padding: 15px;
    }
    .button-group {
      display: flex;
      gap: 10px; /* Adjust gap between buttons */
      justify-content: flex-start; /* Align buttons next to each other */
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <div class="header">
      <h1 class="mb-0">Reviews</h1>
    </div>

    <div id="reviews" class="mt-2">
      <% @reviews.each do |review| %>
        <div class="card">
          <div class="card-body">
            <%= render review %>
            <div class="mt-3 button-group">
              <%= link_to t("show"), review, class: "btn btn-gradient btn-sm" %>
              <% if current_user.user_detail&.is_admin %>
                <%= button_to t("destroy"), review, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-danger btn-sm" %>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Bootstrap JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
